//The module name has to match with the one you put in your route
//the Controller has to be the same name that your provide in the .when( templateUrl: 'Template name',
//controller: 'Controller name'

//Import addition feature put it in the function parmeters
angular.module('Module Name', []).controller('Controller Name', function ($http) {
    //Inside here for the angular code
});
